defaults:
  - dataloader: ???
  - dataset: sharded_dataset
  - training: entitlement
  - checkpoint: none
  - _self_


hydra:
  mode: MULTIRUN

  sweeper:
    params:
      dataloader: s3iterabledataset, fsspec
