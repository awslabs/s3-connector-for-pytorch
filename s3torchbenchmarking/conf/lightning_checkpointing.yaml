defaults:
  - hydra/callbacks:
      - collate_results
  - _self_

s3:
  region: ???
  uri: ???
path: ???

epochs: 5
save_one_in: 1

hydra:
  mode: MULTIRUN
  sweeper:
    params:
      +model: vit-base, whisper, clip-vit, T0_3B, T0pp
      +checkpoint.storage: disk, s3
